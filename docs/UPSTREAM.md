# Upstream seam

`opera-devtools-mcp` is a fork of
[ChromeDevTools/chrome-devtools-mcp](https://github.com/ChromeDevTools/chrome-devtools-mcp).

This document is the registry of every intentional divergence from upstream.
`scripts/verify-upstream-seam.ts` enforces it: unregistered drift in an upstream-owned file fails CI.

- Current fork base: `7a46acf` (upstream, v1.9.0).
- Upstream remote: `git remote add upstream https://github.com/ChromeDevTools/chrome-devtools-mcp.git`

## Intake runbook

```bash
npm install                      # runs `prepare`, which registers the opera-ours merge driver
git fetch upstream --tags
git merge upstream/main          # conflicts only in files registered below
npm run gen                      # regenerate cli-options, tool-reference, metrics
npm run verify-upstream-seam     # fails if an unregistered upstream file drifted
npm test
```

Then update the fork base commit above.

`npm install` is required. The `merge=opera-ours` entries in `.gitattributes` have no effect until
`scripts/setup-intake.ts` registers the driver; git does not warn about an unregistered driver, it
falls back to a normal three-way merge. `npm run verify-upstream-seam` fails when the driver is
missing.

## Design rules

1. **New behaviour goes in `src/opera/**`.\*\* New files never conflict.
2. **Upstream files get references, not rewrites.** Swap a hardcoded literal for a `branding.ts`
   constant, or add a hook call. Never copy an upstream block in order to modify it.
3. **Never fork a file by copying it.** Edit the upstream file minimally in place and make the
   Opera-named path a re-export shim.
4. **Generated artifacts are never hand-merged.** They use the `merge=opera-ours` driver and are
   regenerated by `npm run gen`.
5. **`src/opera/tools/opera.ts` must export only tool definitions.** `createTools` in
   `src/tools/tools.ts` does `Object.values()` over the module, so any other export is treated as a
   tool and breaks the build. Helpers live elsewhere under `src/opera/`.

## Prior art for rule 2

The fork once reverted upstream's `ToolHandler` extraction
([ChromeDevTools/chrome-devtools-mcp#2032](https://github.com/ChromeDevTools/chrome-devtools-mcp/pull/2032))
by copying the invocation path into `src/index.ts` and editing it there. Three regressions shipped as
a result:

1. `--redactNetworkHeaders` was parsed and then ignored — the copy dropped
   `response.setRedactNetworkHeaders(...)`, so headers were always redacted.
2. Disabled-tool CLI guidance was lost, and the e2e assertions were weakened to match.
3. The `opera-devtools` CLI could not invoke any `opera_*` tool: the generator wrote
   `chrome-devtools-cli-options.ts` while the shipped CLI imported a stale hand-maintained
   `opera-devtools-cli-options.ts`.

## Registry

### Opera-owned (new files, never conflict)

| Path                                                                                                                       | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/opera/branding.ts`                                                                                                    | Product/CLI/package names, repo URL, cache dir, index script name, env var keys                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/opera/policy.ts`                                                                                                      | Telemetry + CrUX defaults and inverted help text, `enforceTelemetryPolicy()`, disclaimer text                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/opera/toolHandlerHooks.ts`                                                                                            | The `OperaToolHooks` seam consumed by `src/ToolHandler.ts`: `beforeInvoke`/`afterInvoke` bracket every invocation and claim and release the shared browser (`browserActivity.ts`), and the chunk callback echoes the daemon's streaming token onto every `notifications/message` it sends — and drops one the transport refuses, since an unhandled rejection there is answered by `cleanup(1)`                                                                                                                                                          |
| `src/opera/browserActivity.ts`                                                                                             | Who is inside a tool invocation right now, counted per tool name (`noteToolStarted` / `noteToolFinished` / `otherBrowserUsers`), so the one relaunch that replaces the browser can wait for an idle one instead of closing the pages of whatever is still running                                                                                                                                                                                                                                                                                        |
| `src/opera/browserFlags.ts`                                                                                                | Opera automation launch flags, the single source of truth for launch-vs-attach (`isLaunchMode`, `describeBrowserMode` — the latter reads the daemon's stored argv so `status` can report the mode), and the sticky flag policy: a browser this daemon launched acquires the flags once and keeps them, and the acquisition waits for an idle browser (`browserIdleWaitPolicy`) rather than closing work in progress                                                                                                                                      |
| `src/opera/browserLaunch.ts`                                                                                               | Single source of truth for `ensureBrowserLaunched` options, shared by `src/index.ts` and `browserFlags.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/opera/envConfig.ts`                                                                                                   | Five `OPERA_CLI_*` env vars + `~/.opera-browser-cli/config` loader (quote-aware parse, unknown-key warnings, argv injection); exports `PERSISTENT_PROFILE_IGNORE_DEFAULT_ARGS`, which the stress harness reuses so its persistent-profile scenarios launch exactly like production. An explicit `--isolated`/`--userDataDir` beats the configured profile — injecting both made the MCP server exit during argument parsing                                                                                                                              |
| `src/opera/webStorageWarning.ts`                                                                                           | Removes the `ExperimentalWarning` Node prints when the bundled `debug` reads the `localStorage` global at import time: the guard replaces the lazy accessor with the `undefined` that read produced anyway, leaves a configured `--localstorage-file` alone, and preloads itself into the CLI's children (the daemon, the update check) through `NODE_OPTIONS`                                                                                                                                                                                           |
| `src/opera/config.ts`                                                                                                      | First-run autoconfiguration + config writing (`computeAutoConfig`, `autoConfigure`, `writeConfigFile`, `updateConfigFile`)                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/opera/detect.ts`                                                                                                      | Opera / Opera Neon browser detection (Neon first)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/opera/profile.ts`                                                                                                     | `defaultProfileDir` plus the inspection half `doctor` needs: `inspectProfileLock` (SingletonLock state), `readDevToolsPort` / `parseDevToolsActivePort`, and `probeDevToolsEndpoint`                                                                                                                                                                                                                                                                                                                                                                     |
| `src/opera/pageIdRouting.ts`                                                                                               | Routing `pageId` strip: the CLI never routes by pageId (`withoutRoutingPageId`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/opera/serviceWorkerRetry.ts`                                                                                          | Retry policy for Opera AI dispatches: only the "dispatcher was not able to dispatch" family is replayed (the one failure that proves the action never reached the AI), with a log line per replay — a replay is invisible in the result and its extra tab is the only other trace of it                                                                                                                                                                                                                                                                  |
| `src/opera/daemonLifecycle.ts`                                                                                             | Daemon supervision from the CLI's end: socket-probe liveness, stale-pid process-group reaping and give-up escalation (`ensureCleanStart`), plus the exit-reason file a dying daemon leaves for the next invocation (`writeExitReason` / `readExitReason`)                                                                                                                                                                                                                                                                                                |
| `src/opera/daemonPidFile.ts`                                                                                               | The pid file behind an `O_EXCL` claim, with a TOCTOU-safe reclaim of a dead daemon's file (`claimPidFile`)                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `src/opera/mcpServerSupervisor.ts`                                                                                         | Keeps the daemon's stdio MCP server alive: respawn on the transport's `onclose`, one attempt at a time, and a recorded give-up that tears the daemon down (`superviseMcpServer`)                                                                                                                                                                                                                                                                                                                                                                         |
| `src/opera/daemonSocket.ts`                                                                                                | The daemon's socket edges: `dispatchSocketMessage` answers one frame — a streaming request gets its own token and has its chunks written back as they arrive, and a non-JSON frame — or a handler that throws — becomes an error reply instead of a dead session (`answerSocketMessage`) — and `reportStartupFailure` exits a lost bind race without unlinking the winner's socket                                                                                                                                                                       |
| `src/opera/daemonLog.ts`                                                                                                   | Where the daemon's output goes and what the CLI says about a daemon that is gone: `openDaemonLog` (one-generation rotation, `O_NOFOLLOW`, non-fatal fallback), `daemonExitMessage`, the log paths, and `nameSpawnFailure`                                                                                                                                                                                                                                                                                                                                |
| `src/opera/daemonShutdown.ts`                                                                                              | The daemon's last words: `installShutdownHandlers` owns the signal and uncaught-error handlers (each naming itself in the exit reason), `recordShutdownReason` owns the conditional write                                                                                                                                                                                                                                                                                                                                                                |
| `src/opera/pageRecovery.ts`                                                                                                | Resolving the page a page-scoped tool acts on when the user closes it: re-select a live page, or open the one page the browser is supposed to always have (single-flight). Works through `McpContext`'s public API, which is why the upstream seam is one call site                                                                                                                                                                                                                                                                                      |
| `src/opera/browserErrors.ts`                                                                                               | The browser-lifecycle prose that decides what the user does next: profile-in-use, no-DevTools-endpoint, attach-failed. Keeps `src/browser.ts` at call sites, like the branding strings                                                                                                                                                                                                                                                                                                                                                                   |
| `src/opera/browserCleanup.ts`                                                                                              | `watchBrowserForOrphans` (the call `src/browser.ts` makes after `launch()`: capture the pid, arm the listener) and `killBrowserProcessGroup`, which SIGKILLs the process group so a hard-killed browser's helpers are not left on init. `disarmBrowserOrphanCleanup` holds that kill back for a close we asked for, per launch — a late `disconnected` from it keeps its own flag: Puppeteer emits `disconnected` on a graceful `close()` too, and SIGKILLing a still-flushing profile leaves IndexedDB records with no files behind them                |
| `src/opera/tools/opera.ts`                                                                                                 | The five `opera_*` tools, and `dispatchWithStreamedResponse`'s event plumbing (listeners before dispatch, early-event replay, and the first-event deadline)                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/map-extensions.d.ts`                                                                                                  | `Map.getOrInsert*` shims for chrome-devtools-frontend — **temporary**, drop when the TS lib has them                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `tests/opera/pageRecovery.test.ts`                                                                                         | Unit coverage for the page recovery (re-select, open, single-flight), stub-based so it needs no browser                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/opera/opera.test.ts`                                                                                                | Unit coverage for the Opera tools: payloads and `openFullTabView`, the streamed action's events, the retry policy (what is and is not replayed), and the stalled-action deadline                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/opera/version.test.ts`                                                                                              | Asserts `src/version.ts` matches `package.json` — a release must bump both                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tests/opera/detect.test.ts`, `tests/opera/config.test.ts`                                                                 | Unit coverage for browser detection and first-run autoconfiguration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `tests/opera/profile.test.ts`, `tests/opera/envConfig.test.ts`                                                             | Unit coverage for the profile-dir mapping and the `OPERA_CLI_*` env/config loader                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/opera/pageIdRouting.test.ts`                                                                                        | Unit coverage for the routing-pageId strip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tests/opera/refArgs.test.ts`                                                                                              | The ref naming rule pinned against upstream's descriptions, the translation matrix, and the frame the CLI puts on the socket for `fill @4.11` / `take_screenshot --uid @4.11`                                                                                                                                                                                                                                                                                                                                                                            |
| `src/bin/opera-browser-cli.ts`, `src/bin/opera-devtools-mcp.ts`                                                            | Opera-named entry points: the MCP server is the only declared `package.json` `bin`; the CLI's binstub is deployed by `scripts/link.ts --with-cli`, because npm refuses to write over a binstub owned by another package, and the older `opera-browser-cli` repo owns that name. Both run the env/config preamble from `src/opera/envConfig.ts`, then `import` the upstream file; the CLI also runs `applyEnvToArgv` on its own argv for `start`, so a configured `OPERA_CLI_HEADED`/`OPERA_CLI_USER_DATA_DIR` beats that command's defaults.             |
| `src/bin/opera-devtools-cli-options.ts`, `src/bin/opera-devtools-mcp-cli-options.ts`, `src/bin/opera-devtools-mcp-main.ts` | Compatibility `export *` shims over the upstream files + generator output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/stress/helpers.ts`, `tests/stress/scenarios.ts`                                                                     | Process-lifecycle stress harness and its 12 scenarios (session-tagged process lookup, kill interlock, orphan scans)                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `tests/stress/*.test.ts`, `tests/stress/runner.test.ts`                                                                    | Per-area `node:test` wrappers and the orchestrated stress loop (A1/A2, B1/B2, C1/C2, D1, E2, G1-G3, H1)                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/stress/docker/Dockerfile`, `tests/stress/docker/docker-compose.yml`                                                 | Disposable container for the stress suite (Debian chromium, `procps`, non-root `node` user, `seccomp=unconfined`)                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `scripts/stress-docker.js`                                                                                                 | Builds the compiled suite on the host, resolves a compose frontend (`docker compose` → `docker-compose` → plain build/run) and runs it                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `scripts/link.ts`                                                                                                          | Deploys for `npm run link` / `npm run link:cli`: stops what the previous deployment left running — the CLI daemon, or the MCP server when no daemon runs — then builds and runs `npm link`, which deploys the server alone. `link` leaves the CLI's binstub to the older repo, restoring it if it points into this checkout; `link:cli` deploys that binstub instead, replacing the older repo's. `--prefix` deploys elsewhere (a test seam).                                                                                                            |
| `.dockerignore`                                                                                                            | Build-context pruning for the stress image: 2.3 GB → 35 MB. Must stay at the context root; see the file header for why                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `docs/stress-testing.md`                                                                                                   | Operator guide: running the stress suite locally, in the container, and on CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `docs/specs/stress-test-system-plan.md`, `docs/specs/migration-poc-phase-1.md`                                             | Opera-owned specs (design history + as-built reference; migration PoC)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `STRESS_TEST_ROBUSTNESS_PLAN.md`                                                                                           | The robustness plan the six supervision mechanisms below implement — kept at the repo root as the working document for this fix, and the record of which defect each mechanism closes                                                                                                                                                                                                                                                                                                                                                                    |
| `.github/workflows/stress-test.yml`                                                                                        | `workflow_dispatch`-only CI job for the stress suite (non-blocking until the robustness fixes land)                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/opera/compactSnapshot.ts`                                                                                             | Verbatim port of opera-browser-cli's `snapshot.ts`: ~19 ordered transforms from the MCP a11y tree to the compact form, plus tree truncation and the URL lookup table (`applyUrlLut` / `resolveUrl` / `absolutizeUrl`, the inverse of `cleanUrl`'s origin strip) — one deliberate deviation: `resolveUrl` does not strip a leading `@` before the `$uN` check, so a token and an element ref cannot be confused for one another                                                                                                                           |
| `src/opera/suggestions.ts`                                                                                                 | Ported from opera-browser-cli's `suggestions.ts`: context-aware next-step hints. Two deviations: the binary name comes from `branding.ts`, and the submit-label pattern is word-anchored — the source's unanchored one read `Designer`, `Google` and `Booking` as submit buttons                                                                                                                                                                                                                                                                         |
| `src/opera/cliOutput.ts`                                                                                                   | The CLI's output path: `renderHelp` / `renderOutput` / `renderError`, TOON encoding (lazy peer-dep load), the snapshot pipeline (`formatToolResult`) and its page-metadata block (`title` / `url` / `refs` — the URL a navigating command went to, else the page's own, read from the raw tree), the error→exit-code renderer, and the `last-url-map[-<sessionId>].json` sidecar `url` reads back — the token map plus the page origin its compact values were shortened against (per session, so two sessions cannot answer each other's token lookups) |
| `src/opera/cliCommands.ts`                                                                                                 | The fork's half of the CLI's command surface, so `src/bin/chrome-devtools.ts` holds a call and a one-line loop instead of ~150 lines: `registerOperaCommands` (`setup`/`doctor`/`logs`/`url`, plus the argv re-expansion the ported parsers read) and `registerToolCommand` (one generated tool definition → a yargs command, with the exit-code mapping and the streaming sink). `start` is injected, not reimplemented                                                                                                                                 |
| `src/opera/cdpErrors.ts`                                                                                                   | The exit-code contract (`ErrorCode`, `EXIT_CODES`), `CdpError` (a plain `Error` where the source extended `axi-sdk-js`), the Opera AI result-error descriptors (including the browser's unreadable AI store and the stalled action the tool reports itself), and message-based failure classification — which repeats those last two shapes as patterns, since a tool that raises them never reaches the descriptors                                                                                                                                     |
| `src/opera/streamingTools.ts`                                                                                              | The six long-running Opera AI tools: streamed output, the 20-minute timeout, the no-replay set, and the deadline for a streamed action that reports nothing at all (`operaAiStreamPolicy`) — plus the `isOperaAiTool` / `operaAiTimeoutMs` questions both ends of the chain have to answer the same way                                                                                                                                                                                                                                                  |
| `src/opera/daemonStreaming.ts`                                                                                             | Both sides of the streaming protocol: the MCP-client notification handler that catches `notifications/message` chunks, the chunk sinks keyed by the streaming token of the request that produced them (so two concurrent commands cannot take each other's output down), and the client-side reader (`requestOverSocket`) that pulls chunks off a socket until the final response frame                                                                                                                                                                  |
| `src/opera/daemonToolCall.ts`                                                                                              | The daemon's `tools/call` policy in one place: the per-tool timeout (`operaAiTimeoutMs`) and the streaming token the request carries in `_meta`. Kept out of `daemon.ts`, which is injections only                                                                                                                                                                                                                                                                                                                                                       |
| `src/opera/setup.ts`                                                                                                       | `setup`: interactive and non-interactive configuration, and the `SKILL.md` install into `~/.claude/skills/` and `~/.agents/skills/`                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/opera/doctor.ts`                                                                                                      | `doctor`: the five checks that map onto the daemon model (config, executable, profile lock, daemon, log), the two mechanical repairs, and `--fix`                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/opera/logs.ts`                                                                                                        | `logs`: tail / `--errors` / `--follow` over the daemon's own log, with the source's error pattern and rotation-aware follow                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/opera/urlResolver.ts`                                                                                                 | `url`: resolve a `$uN` token from this session's sidecar (no round-trip) or a ref (`@1.1`, `1_1` — the wire form `--raw` prints — or `1.1`) against a fresh snapshot, to the full URL with the page origin re-attached to the compacted value; the two shapes are disjoint, so `@$u2` is a ref lookup and not a token one                                                                                                                                                                                                                                |
| `src/opera/refArgs.ts`                                                                                                     | Ref arguments: `refArgNames` (the generated table's `uid` / `*_uid` arguments, pinned against upstream's descriptions by `tests/opera/refArgs.test.ts`) and `normalizeRefArgs` — the source CLI's per-command `parseUid` (`@` off, `dot → underscore`) applied once where the generated command surface builds the tool call, so the `@4.11` a snapshot prints reaches the MCP tools as `4_11`                                                                                                                                                           |
| `src/opera/skills/SKILL.md`                                                                                                | The agent skill for the CLI, kept in `src/opera/skills/` and copied to `build/src/opera/skills/` by `scripts/post-build.ts` so it publishes with the rest of `build/src`; `setup` installs it for Claude Code and the generic agent path. Content re-pointed at this fork's actual command surface                                                                                                                                                                                                                                                       |
| `tests/opera/compactSnapshot.test.ts`                                                                                      | Ported from opera-browser-cli's `test/snapshot.test.ts` (vitest → `node:test`); 104 cases over every transform, the LUT, trailer ordering, and URL resolution                                                                                                                                                                                                                                                                                                                                                                                            |
| `tests/opera/suggestions.test.ts`                                                                                          | Ported from opera-browser-cli's `test/suggestions.test.ts`, plus the branding assertion                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/opera/cliOutput.test.ts`                                                                                            | Unit coverage for the compaction pipeline, `--raw` / `--full`, the TOON document, the per-session sidecar round-trip, and the exit-code table                                                                                                                                                                                                                                                                                                                                                                                                            |
| `tests/opera/streaming.test.ts`                                                                                            | Unit coverage for the multi-frame socket protocol (with the connection stubbed, since a sandbox may deny `AF_UNIX` binds), daemon-side chunk forwarding and its token routing under two concurrent requests, the chunk notification the server sends, the no-replay invariant, every CDP error code, and a chunk the transport refuses (dropped, not raised)                                                                                                                                                                                             |
| `tests/opera/daemonToolCall.test.ts`                                                                                       | Unit coverage for the daemon's `tools/call` policy: the long timeout on the AI tools, the SDK default for the rest, the token in `_meta`, and a failed call surfacing to the daemon                                                                                                                                                                                                                                                                                                                                                                      |
| `tests/opera/setup.test.ts`                                                                                                | Unit coverage for `parseSetupArgs` (ported) and the non-interactive config + `SKILL.md` install                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tests/opera/webStorageWarning.test.ts`                                                                                    | Unit coverage for the guard: a CLI command, the MCP server and a child that inherits the CLI's preload stay silent, and a configured `--localstorage-file` is not clobbered                                                                                                                                                                                                                                                                                                                                                                              |
| `tests/opera/doctor.test.ts`                                                                                               | Unit coverage for all five checks, the `--fix` repairs, and log rotation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tests/opera/logs.test.ts`                                                                                                 | Unit coverage for the arg parser, the error filter, and tail / `--lines` / `--errors` output                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/opera/urlResolver.test.ts`                                                                                          | Unit coverage for the sidecar fast path, ref resolution, the stale-ref exit code, and the token/ref split                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/opera/daemonSocket.test.ts`                                                                                         | Unit coverage for the daemon's socket edges: `answerSocketMessage` on a frame that is not JSON and on a handler that throws                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `tests/opera/browserCleanup.test.ts`                                                                                       | Unit coverage for the group kill and its disarm: a browser that dies on its own is killed, a deliberate close is not, and the previous launch's watcher keeps its own disarm when the next one arms                                                                                                                                                                                                                                                                                                                                                      |
| `tests/opera/browserFlags.test.ts`, `tests/opera/browserLaunch.test.ts`                                                    | Unit coverage for the automation-flag policy (which tool needs which flag) and for the launch options `src/index.ts` and `browserFlags.ts` share                                                                                                                                                                                                                                                                                                                                                                                                         |
| `tests/opera/mcpServerSupervisor.test.ts`                                                                                  | Unit coverage for the MCP server supervision: respawn once per close, ignore a replaced server's close, and give up rather than loop                                                                                                                                                                                                                                                                                                                                                                                                                     |

### Upstream files we modify (keep these edits small)

| Path                                  | Divergence                                                                                                                                                                                                                                                                                                                                             | Permanent?                        |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------- |
| `src/ToolHandler.ts`                  | Optional 5th ctor arg `hooks`; `handle(params, extra)`; three `this.hooks?.` call sites; `guard?.dispose()`; CLI name in `buildDisabledMessage`; one page-resolution line calling `opera/pageRecovery.ts`                                                                                                                                              | yes                               |
| `src/index.ts`                        | Builds `operaHooks`, passes them to `ToolHandler`, threads `extra`; `logDisclaimers` uses `opera/policy.ts`; `getContext` builds launch options via `opera/browserLaunch.ts`                                                                                                                                                                           | yes                               |
| `src/McpPage.ts`                      | `resolveElementHandle` reports failed lookups separately from resolved-but-absent elements                                                                                                                                                                                                                                                             |
| `src/tools/categories.ts`             | `OPERA` enum member + label                                                                                                                                                                                                                                                                                                                            | yes                               |
| `src/tools/tools.ts`                  | Registers the Opera tools                                                                                                                                                                                                                                                                                                                              | yes                               |
| `eslint.config.js`                    | `enforce-zod-schema` glob also covers `src/opera/tools/**`                                                                                                                                                                                                                                                                                             | yes                               |
| `src/daemon/daemon.ts`                | Injections, no logic — each one a call into `src/opera/` that returns: pid claim, supervisor + its `onclose` wiring, the frame answer (`dispatchSocketMessage`: it mints the streaming token and holds its `withLogSink`), startup failure, shutdown handlers and reasons, `attachLogForwarding` for streaming, and `callDaemonTool` for the tool call | yes                               |
| `src/daemon/client.ts`                | `startDaemon` uses `ensureCleanStart` instead of the pid-file liveness check, takes the spawn's log stdio from `opera/daemonLog.ts` and races the pid-file wait against `nameSpawnFailure`; `sendCommand` adds the exit reason and the optional `onLog` sink, whose streaming frame protocol is `requestOverSocket` (`opera/daemonStreaming.ts`)       | yes                               |
| `src/browser.ts`                      | Adds `getCurrentBrowser()` / `closeBrowserIfOpen()` exports alongside upstream's `closeBrowser`; one `watchBrowserForOrphans(launched)` call after `launch()`; `disarmBrowserOrphanCleanup()` at the top of both closing paths, so a deliberate close is not answered with the group kill; three lifecycle messages from `opera/browserErrors.ts`      | yes                               |
| `src/McpResponse.ts`                  | `sendLog()` + optional `logCallback` ctor arg; the selection note also covers a page the product opened itself                                                                                                                                                                                                                                         | yes (local only)                  |
| `src/tools/ToolDefinition.ts`         | `sendLog` on `Response`; `signal?: AbortSignal` on `Request`                                                                                                                                                                                                                                                                                           | yes (local only)                  |
| `src/daemon/utils.ts`                 | App name + index script path from branding                                                                                                                                                                                                                                                                                                             | yes                               |
| `src/utils/check-for-updates.ts`      | Env key + cache dir from branding                                                                                                                                                                                                                                                                                                                      | yes                               |
| `src/bin/check-latest-version.ts`     | Package name from branding (keep upstream's `getRegistry()`)                                                                                                                                                                                                                                                                                           | yes                               |
| `src/config/mcp-options.ts`           | Branding strings; `performanceCrux` + `usageStatistics` default/help text from `opera/policy.ts`                                                                                                                                                                                                                                                       | yes                               |
| `src/config/browser-options.ts`       | Branding references (`REPO_URL`, `CACHE_DIR_NAME`, `MCP_BIN_NAME`) in the browser-option help text                                                                                                                                                                                                                                                     | yes                               |
| `src/config/category-options.ts`      | Adds the `ToolCategory.OPERA` entry so the generated category flags include `categoryOpera`                                                                                                                                                                                                                                                            | yes                               |
| `src/bin/chrome-devtools-mcp-main.ts` | Branding strings; calls `enforceTelemetryPolicy()` from `opera/policy.ts`                                                                                                                                                                                                                                                                              | yes                               |
| `src/bin/chrome-devtools.ts`          | Branding strings; a parse failure exits with the fork's codes; `status` prints `browser=…` from `describeBrowserMode`; its four extra commands and the generated-tool wrapper are registered by `src/opera/cliCommands.ts`, which owns the pageId strip, the exit-code mapping and the streaming plumbing                                              | yes                               |
| `src/bin/chrome-devtools-mcp.ts`      | Branding strings (`process.title`, Node-version error text)                                                                                                                                                                                                                                                                                            | yes                               |
| `src/telemetry/transformation.ts`     | Adds `ZodRecord` to the supported telemetry zod types (used by `parameters` on `opera_call_mcp_tool`); maps it to a `_count` metric                                                                                                                                                                                                                    | yes                               |
| `scripts/generate-cli.ts`             | Opera attribution in the file header and in the generated-file header template                                                                                                                                                                                                                                                                         | yes                               |
| `scripts/post-build.ts`               | Adds `makeBinScriptsExecutable()` so generated JS bin scripts stay executable (tsc does not preserve file permissions)                                                                                                                                                                                                                                 | yes                               |
| `scripts/verify-npm-package.js`       | Also requires `build/src/opera/skills/SKILL.md`, so a publish that stops copying the CLI's skill to `build/src` fails the package check                                                                                                                                                                                                                | yes                               |
| `scripts/test.js`                     | Opera env var keys                                                                                                                                                                                                                                                                                                                                     | yes                               |
| `tsconfig.json`                       | `ESNext.Iterator`/`ESNext.Collection` → `ES2025.*`                                                                                                                                                                                                                                                                                                     | **temporary** — TS lib workaround |
| `src/third_party/index.ts`            | Re-exports `CDPSessionEvent` and `TargetCloseError` from puppeteer-core's internal modules (the public re-export strips `@internal` members), and `LoggingMessageNotificationSchema` so the daemon can register a `notifications/message` handler                                                                                                      | yes                               |
| `src/daemon/types.ts`                 | `stream?: true` on `invoke_tool` (opt-in multi-frame responses) + the `DaemonLogFrame` type                                                                                                                                                                                                                                                            | yes                               |
| `tests/browser.test.ts`               | Asserts the properties the rewritten profile-in-use message must have (names the profile, offers both remedies) instead of upstream's exact wording                                                                                                                                                                                                    | yes                               |
| `tests/daemon/client.test.ts`         | Adds coverage for the daemon exit-reason file and the log path, alongside upstream's client cases                                                                                                                                                                                                                                                      | yes                               |

### Upstream tests we modify

These are name changes, not behaviour changes: the fork's binary, package and env-var names differ,
so the assertion strings do too. A test that needs a behavioural change indicates the seam is in the
wrong place.

| Path                                        | Divergence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `tests/utils.ts`                            | `CLI_PATH` resolves `build/src/bin/opera-browser-cli.js` (`CLI_BIN_NAME`); daemon status strings use the `opera-devtools-mcp` name; `runCli` spawns through `createCliEnv()` — **behavioural**: throwaway `HOME`, every `OPERA_CLI_*` stripped, `OPERA_CLI_EXECUTABLE_PATH` set to the bundled Chrome, so a real `~/.opera-browser-cli/config` cannot leak into the suite                                                                                                                                                                                                                                                                                    |
| `tests/cli.test.ts`                         | Opera package name; `performanceCrux`/`usageStatistics` default to `false`; `categoryOpera` flag on by default                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `tests/index.test.ts`                       | Opera bin path + env var key; child env comes from `createCliEnv()` (isolated `HOME`, `OPERA_CLI_*` stripped) so no real config leaks in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `tests/utils/check-for-updates.test.ts`     | Opera env var key; drops upstream's downgrade case (see `src/utils/check-for-updates.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tests/daemon/utils.test.ts`                | `APP_NAME` uses the Opera package name                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tests/ToolHandler.test.ts`                 | Opera env var keys, plus coverage for the `OperaToolHooks` seam; page resolution recovers a closed selection, and an explicitly named page is not recovered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/telemetry/transformation.test.ts`    | Adds coverage for `ZodRecord` telemetry handling (count of record keys)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `tests/McpPage.test.ts`                     | Adds coverage for `resolveElementHandle` distinguishing a rejected lookup from a resolved-but-absent element                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tests/e2e/opera-devtools-*.test.ts`        | `describe`/assertions use the fixed `CLI_BIN_NAME` (`opera-browser-cli`); the routing pageId positional is never passed — the CLI strips it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/e2e/opera-devtools-commands.test.ts` | **behavioural**: five cases assert exit 2 where upstream asserted 0 — every one is a failed tool result (`list_network_requests` and `click_at` disabled, `evaluate_script` disabled, a refused `javascript:` navigation, an unknown tool argument). The fork maps a failed tool result through `opera/cdpErrors.ts` to the documented exit code. The stress harness (`tests/stress/scenarios.ts`, H1) depends on the same contract: a step it asserts exits 0 must be a call that succeeds, so H1 opens its second page with a block-bodied script (`evaluate_script` stringifies the return value, and `window.open()` returns an unserializable `Window`) |

### Upstream files we rename or delete

The merge driver does not apply to these. Git reports a rename/modify or delete/modify conflict;
resolve by hand, keeping the Opera side. Git leaves upstream's copy in the tree when it does, so a
reflexive `git add -A` brings the file back — `npm run verify-upstream-seam` fails if any path in
this table reappears.

| Path                                                                                                                                                                                      | What we did                                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `tests/e2e/chrome-devtools-commands.test.ts`, `tests/e2e/chrome-devtools-disclaimers.test.ts`, `tests/e2e/chrome-devtools-start-stop.test.ts`, `tests/e2e/chrome-devtools-status.test.ts` | Renamed to `opera-devtools-*`; they drive the sole Opera-named CLI (`CLI_BIN_NAME`) |
| `tests/e2e/telemetry.test.ts`                                                                                                                                                             | Deleted — Opera forces telemetry off, so there is no upload path left to assert     |
| `AGENTS.md`                                                                                                                                                                               | Deleted in favour of Opera's own agent docs                                         |
| `server.json`, `scripts/verify-server-json-version.ts`, `.github/workflows/publish-to-mcp-registry-on-tag.yml`                                                                            | Deleted — the fork publishes to npm only, never to the MCP registry                 |
| `.release-please-manifest.json`, `release-please-config.json`                                                                                                                             | Deleted — the fork releases via its own publish-on-tag workflow, not release-please |

### Generated — never hand-merge, always regenerate

`src/config/cli-options.ts`, `src/telemetry/tool_call_metrics.json`,
`src/telemetry/flag_usage_metrics.json`, `docs/tool-reference.md`, `docs/slim-tool-reference.md`,
`docs/configuration.md`, `package-lock.json`.

### Opera-owned prose / config (merge=opera-ours)

`README.md`, `CHANGELOG.md`, `.gitignore`, `.gitattributes`, `docs/troubleshooting.md`,
`docs/UPSTREAM.md`, `docs/architecture.md`, `docs/specs/*`, `package.json`, `.github/workflows/*`,
`.github/plugin/plugin.json`, `src/version.ts`, `.claude-plugin/*`, `NOTICE`.

`src/version.ts` carries the fork's own version line, which is independent of upstream's.
The fork publishes to npm on release tags via the `publish-on-tag` workflow (OIDC).
It does not publish to the MCP registry: upstream's registry workflow, `server.json` and the
`verify-server-json-version` script are deleted, so a release bumps only `package.json` and
`package-lock.json`.

## Deliberately NOT upstreamed

`sendLog` / MCP logging notifications and `signal` on `Request` are generic enough to
upstream but are kept local by decision. If upstream adds equivalents, delete ours
rather than keeping both. (The `WaitForHelper` dialog fix was upstreamed as of
the 1.7.0 intake — the fork's older local copy was deleted.)

## Measuring the seam

Intake cost is approximated per upstream-owned file under `src/` and `scripts/` as the lines we
changed multiplied by the number of upstream commits touching that file since the previous fork base.
Files we changed heavily that upstream also changes often dominate the sum.

```bash
git diff --name-only <fork-base> -- src scripts | while read f; do
  git cat-file -e "<fork-base>:$f" 2>/dev/null || continue   # upstream-owned only
  ours=$(git diff --numstat <fork-base> -- "$f" | awk '{print $1+$2}')
  churn=$(git log --oneline <previous-fork-base>..upstream/main -- "$f" | wc -l | tr -d ' ')
  echo "$f|$ours|$churn|$((ours*churn))"
done | sort -t'|' -k4 -rn
```

At the `7a46acf` base, after the refactor that moved the CLI's command surface and both ends of the
socket protocol behind `src/opera/cliCommands.ts`, `daemonStreaming.ts` and `daemonSocket.ts`:
**1547** across 30 files, down from **1750** across the same 30 before it. The three heaviest upstream
files — `src/bin/chrome-devtools.ts`, `src/daemon/daemon.ts`, `src/daemon/client.ts` — went from
**591** to **388** changed lines between them, and what is left of each is a call into `src/opera/`
rather than the code behind it. About a third of the remainder (`src/config/cli-options.ts`,
`src/telemetry/tool_call_metrics.json`, 673 together) is generated and resolves via `npm run gen`; a
further 77 is the deleted `scripts/verify-server-json-version.ts`, which the delete table owns. That
leaves a hand-merge surface of roughly 800. Re-measure after each intake. A rising score means a
divergence has grown that belongs behind a seam in `src/opera/`.
